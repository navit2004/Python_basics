# 4  Replace the double space from problem 3 with single spaces.
name="Navit is a good  boy and"

print(name.replace("  "," ")) #Strings are immutable which means that you cannot change them by running functions on them.